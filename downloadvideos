#!/bin/bash
if [ $# -eq 1 ]
mkdir flvs
cd flvs
then
  # download each file in the list
  while read -r line; do wget -b -nc "$line"; echo $line >> ../download.log; done < ../flv-lists/$1
fi
mail -s "Download complete" oi.markwilliams@gmail.com < ../download.log
